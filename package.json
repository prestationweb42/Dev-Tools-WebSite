{
    "devDependencies": {
        "gh-pages": "^5.0.0"
    },
    "homepage": "https://github.com/prestationweb42/Dev-Tools-WebSite",
    "scripts": {
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build"
    }
}
